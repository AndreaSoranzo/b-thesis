#import "utility.typ": date, myName, myTitle, slide, startSlide, unipdColor
#set document(date: date, title: myTitle, author: myName)

#startSlide(
  myTitle,
  myName,
  date,
)

#slide("ORDINE DI ESPOSIZIONE", [
  = PROVA
])

